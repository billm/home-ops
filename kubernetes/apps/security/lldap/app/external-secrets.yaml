---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: lldap-secret
  namespace: flux-system
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    creationPolicy: Owner
  data:
    - secretKey: lldap-secret
      remoteRef:
        key: lldap-admin
        property: password
    - secretKey: lldap-secret
      remoteRef:
        key: lldap-admin
        property: jwt-secret
